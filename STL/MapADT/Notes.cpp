//! Our goal is to reaplciate the map data structure in our own way.

//! Before dive deeply into the Map data structure we need to have a idea about how the map works 

// ! Map is data structure that used to store key value pairs , similar to the JSON , dictonary in python , object literal in js

//! Now we know what is Map, we can dive into the operation on Map 👇

 

//* Method	                                    Purpose
//  containsKey(const K& key)	            Checks if a key exists
//  get(const K& key)                       Returns value for a key (throws if not found)
//  getSize()	                            Returns current size
//  isEmpty()	                            Checks if map is empty
//  clear()	                                Removes all key-value pairs
//  update( key,  value)	Updates the value for an existing key
//  capacity()	                            Returns capacity (optional for debugging)


//? Map is ds the shrink, increase its size 

