#include<iostream>
using namespace std;


int main(){

    return 0;
}
/**
 * ? Introduction
 * ! Data structure: It is the arrangements of data  and the operations on data, so that we can use the data more efficiently
 * ! Where ? -> inside the main memory during the execution of the program
 * ! During the execution of the program the how the program wil manage the data inside the main memory
 * 
 */